{
  "name": "beam-for-individuals",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm start --prefix server",
    "dev": "npm run dev --prefix server",
    "install": "npm install --prefix server"
  }
}